import type { Metadata } from "next";
import  RootLayout from "@/app/root-layout";

export const metadata: Metadata = {
    title: "Welcome | Mtaani FX",
    description: "Invest in your future",
};

export default RootLayout;
